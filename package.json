{
  "name": "husbandlabs",
  "version": "0.1.0",
  "private": true,
  "description": "HusbandLabs — static landing page deployed to Cloudflare Pages",
  "scripts": {
    "dev": "wrangler pages dev public",
    "deploy": "wrangler pages deploy public --project-name husbandlabs --branch master"
  },
  "devDependencies": {
    "wrangler": "^4.61.1"
  }
}
